import { getAssets } from './lib/inputs.js'
import {
  N_TRIPLES,
  toFile,
  toStream,
  toString,
  TRIG,
  TURTLE,
} from './lib/outputs.js'

export { getAssets, toString, toStream, toFile, TURTLE, TRIG, N_TRIPLES }
